rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Deny all read and write access
    match /{document=**} {
      allow read, write: if false;
    }

    match /test_connection/ping {
      allow get: if request.auth != null;
    }
  }
}